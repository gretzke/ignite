{
  "name": "@ignite/api",
  "version": "1.0.0",
  "description": "Ignite shared API contracts",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "nodemon --watch src --ext ts,tsx,js,json --ignore \"dist/**\" --exec \"npm run build\"",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ignite/plugin-types": "file:../../plugins/src/shared/dist/",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.9",
    "typescript": "^5.8.3"
  }
}
