{
  "name": "@ignite/hook-deployer",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && mkdir -p dist && esbuild index.ts --bundle --platform=node --target=node22 --format=esm --minify --banner:js=\"import { createRequire as __createRequire } from 'module'; const require = __createRequire(import.meta.url);\" --outfile=dist/index.js --define:PLUGIN_VERSION='\"'$(node -p \"require('./package.json').version\")'\"'"
  },
  "devDependencies": {
    "@types/node": "22.0.0",
    "esbuild": "0.25.8",
    "typescript": "5.8.3"
  }
}
