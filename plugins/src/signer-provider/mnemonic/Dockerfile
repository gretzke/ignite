# Ignite RPC Provider Infura
FROM ignite/shared:latest

# Pure-JS plugin: it only maps a vaulted API key onto per-chain URL strings,
# so the shared base (node, unprivileged plugin user) is all it needs — no
# toolchain install on top.

# Container starts in idle mode, waiting for rpc-provider plugin JS
CMD ["sleep", "infinity"]
